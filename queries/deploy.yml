# helium-queries push → webhook always updates bare-metal DEV (:8137) immediately.
# Vercel = production (state_of_solana GitHub → Vercel build).
#
#   false → dev on :8137 only (default)
#   true  → dev on :8137 + commit/push state_of_solana → Vercel prod
deploy on vercel: false
