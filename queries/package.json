{
  "name": "helium-queries",
  "version": "1.0.0",
  "private": true,
  "description": "Helium Oracle SQL catalog and JSON API (standalone or embedded in State of Solana)",
  "main": "app/server.js",
  "scripts": {
    "start": "node app/server.js",
    "catalog:build": "node app/catalog/generate-queries-index.js",
    "webhook": "node app/webhook-listener.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "dotenv": "^16.4.5"
  }
}
