# Standalone server
HELIUM_APIS_PORT=8138
SITE_URL=http://localhost:8138

# Optional: proxy query execution to another host (no local Python/Trino)
# HELIUM_QUERY_PROXY_ORIGIN=http://84.32.71.101:8137

# When embedded under state_of_solana/queries (set automatically by sync script)
# HELIUM_MONOREPO_ROOT=/path/to/state_of_solana

# Trino (via parent monorepo pipeline or local install)
# TRINO_HELIUM_CATALOG=hive
# TRINO_HELIUM_SCHEMA=helium

# GitHub webhook (app/webhook-listener.js)
# HELIUM_WEBHOOK_PORT=9001
# HELIUM_WEBHOOK_SECRET=
