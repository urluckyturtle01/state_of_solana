name: Deploy Next.js to GitHub Pages

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "18"
          cache: 'npm'
      
      - name: Install dependencies
        run: npm ci --legacy-peer-deps
      
      - name: Disable dynamic API routes
        run: |
          find ./app/api -type f -name "route.ts" -exec sed -i 's/export const dynamic = "force-dynamic";/\/\/ export const dynamic = "force-dynamic";/g' {} \;
          find ./app/api -type f -name "route.tsx" -exec sed -i 's/export const dynamic = "force-dynamic";/\/\/ export const dynamic = "force-dynamic";/g' {} \;
      
      - name: Update next.config.js for static export
        run: |
          cat > next.config.js << 'EOF'
          /** @type {import('next').NextConfig} */
          const nextConfig = {
            output: 'export',
            basePath: '/state_of_solana',
            images: {
              unoptimized: true,
            },
            reactStrictMode: false,
            eslint: {
              ignoreDuringBuilds: true,
            },
          };
          
          module.exports = nextConfig;
          EOF
      
      - name: Build
        run: npm run build
        env:
          NEXT_DISABLE_ESLINT: 1
      
      - name: Create .nojekyll
        run: touch out/.nojekyll
      
      - name: Create static API responses
        run: |
          # Create necessary API directories
          mkdir -p out/state_of_solana/api/charts
          mkdir -p out/state_of_solana/api/chart-data
          
          # Create a static response for /api/charts
          cat > out/state_of_solana/api/charts/index.html << 'EOF'
          {"charts":[]}
          EOF
          
          # Create static responses for chart-data
          cat > out/state_of_solana/api/chart-data/index.html << 'EOF'
          {"message":"Static API response"}
          EOF
      
      - name: Setup Pages
        uses: actions/configure-pages@v4
      
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./out

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4